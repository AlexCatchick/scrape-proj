# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ['nodejs-20_x']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node .next/standalone/server.js'

[variables]
NODE_ENV = 'production'
HOSTNAME = '0.0.0.0'
